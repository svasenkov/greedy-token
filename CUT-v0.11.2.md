# Cut checklist — greedy-token v0.11.2

**Status:** FOLDED into **v0.13.0** (SHIPPED 2026-07-30) — no separate tag.

Patch intent: pin `mcp` so CI/import keeps `mcp.server.fastmcp`; honest Trust
cut A docs already on `main`.

## Summary

- **mcp pin**: optional extra `mcp>=1.0,<2` (mcp 2.0 removed `fastmcp`).
- **docs**: README/WHY/guide honest savings framing (★ $82/$820 illustrative).

## CONTRACT — evidence

| Claim | Evidence |
|-------|----------|
| pin | `pyproject.toml` optional `mcp = ["mcp>=1.0,<2"]` |
| docs | README / WHY / ROADMAP Trust cut A |

Shipped as part of `v0.13.0` together with v0.12 edit-escalation and v0.13
corpus/shell harden. See `CUT-v0.13.0.md`.
