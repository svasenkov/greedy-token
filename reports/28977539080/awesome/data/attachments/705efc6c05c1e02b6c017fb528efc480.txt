RAG hits for: baseUrl -D flag

1. [test-baseurl] score=4.0  (e2e)
   docs/rag/e2e/test-chunk.md

baseUrl is configured via -DbaseUrl flag in Gradle.

---
---
Token economy

This call
  Executor: rag — docs/rag chunk read
  Route: mcp-rag
  Duration: 137 ms
  Spent (MCP executor, LLM tokens): ~16  (docs/rag chunks read into context)
  Billing: read docs/rag, 1 chunk(s) — small context vs full agent chat

Cursor agent chat (naive — same task, no MCP tool)
  Always-on rules: ~6
  Task prompt:     ~11
  Agent overhead:  ~6,000
  Total (naive agent chat):  ~6,017

Tier alternatives (estimated):
  rg (local search)          ~     0  · 0 LLM
  python (script)            ~     0  · 0 LLM
  ollama (local LLM)         ~ 6,011  · unavailable (would fall back to cursor)
  rag (docs/rag read)        ~ 1,811  ← this call
  cursor (agent / cloud)     ~ 6,017

Saved vs naive Cursor chat
  Baseline (naive agent chat):  ~6,017
  Spent (MCP executor, LLM tokens): ~16  (docs/rag chunks read into context)
  Saved:             ~6,001  (= baseline − spent)

Note: MCP in Agent chat still uses Cursor tokens for rules + your message +
agent reply. Only the executor row above is local / cheap.