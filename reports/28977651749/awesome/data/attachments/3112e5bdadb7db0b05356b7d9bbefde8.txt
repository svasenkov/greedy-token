Pipeline: search baseUrl	sample.js then rag baseUrl
Steps: 2

── Step 1/2: search: baseUrl	sample.js [tool/ran] OK · 2ms · ~0 tok
Search: 'baseUrl' in projects/sample.js [python]
(rg not in PATH — python file scan)

/tmp/pytest-of-runner/pytest-0/test_cli_pipeline_execute_sear0/projects/sample.js:1:const baseUrl = 'http://localhost';

── Step 2/2: rag: baseUrl [rag/ran] OK · 0ms · ~13 tok
RAG hits for: baseUrl

1. [test-baseurl] score=3.0  (e2e)
   docs/rag/e2e/test-chunk.md

baseUrl is configured via -DbaseUrl flag in Gradle.

---
---
Token economy — pipeline

Per-step savings (if each step were a separate naive Cursor chat):
   #  step                   executor     ms   spent  baseline     saved  billing
   1  search                 tool          2       0     6,012     6,012  local script — no cloud LLM
   2  rag                    rag           0      13     6,009     5,996  docs/rag chunks read into context

Saved by executor (sum of per-step savings):
  rg (local search)            steps=1  spent ~0  saved ~6,012
  rag (docs/rag read)          steps=1  spent ~13  saved ~5,996

Run log:
  step                         tier         ms   tokens  status
  search                       tool          2        0 OK
  rag                          rag           0       13 OK

Spent by executor:
  rg (local search) (0 cloud LLM)  steps=1  ~0 tok
  rag (docs/rag read)              steps=1  ~13 tok

Pipeline total: 0.0 s · ~13 LLM tokens spent

Combined naive Cursor chat (whole pipeline as one agent task)
  Always-on rules: ~6
  Task prompt:     ~8
  Agent overhead:  ~6,000
  Total (naive agent chat):  ~6,014

Saved vs naive Cursor chat
  Baseline (naive agent chat):  ~6,014
  Spent (MCP executor, LLM tokens): ~13  (sum of pipeline steps)
  Saved:             ~6,001  (= baseline − spent)

Note: Per-step baseline assumes a separate agent chat per step (rules+overhead each time).
Pipeline total baseline = one agent chat for the full pipeline task.
Agent wrapper (MCP + reply) still uses Cursor tokens beyond executor rows.
