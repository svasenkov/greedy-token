Task: find baseUrl in sample.js
Route: TOOL  (tool-rg-search, 59%)
Complexity: low
Est. tokens: 0
Rationale: Mechanical search — ripgrep/jq, zero LLM tokens. 0 tokens — ripgrep before RAG/Cursor
Matched: find
Command: cd /tmp/pytest-of-runner/pytest-0/test_mcp_stdio_route_includes_0 && rg -n --max-columns 200 -F baseUrl -g '!.git/**' -g '!node_modules/**' -g '!build/**' -g '!.venv/**' -g '!.cursor/hooks/**' --max-count 50 projects docs stacks scripts generators
Execute: read-only (greedy-token run --execute OK)
---
Token economy

This call
  Executor: rg — ripgrep on disk
  Route: tool-rg-search
  Duration: 147 ms
  Spent (MCP executor, LLM tokens): ~0  (ripgrep on disk — no cloud LLM)
  Billing: local only — no Cursor cloud LLM for this step

Cursor agent chat (naive — same task, no MCP tool)
  Always-on rules: ~6
  Task prompt:     ~5
  Agent overhead:  ~6,000
  Total (naive agent chat):  ~6,011

Tier alternatives (estimated):
  rg (local search)          ~     0  ← this call
  python (script)            ~     0  · 0 LLM
  ollama (local LLM)         ~ 6,005  · unavailable (would fall back to cursor)
  rag (docs/rag read)        ~ 1,805
  cursor (agent / cloud)     ~ 6,011

Saved vs naive Cursor chat
  Baseline (naive agent chat):  ~6,011
  Spent (MCP executor, LLM tokens): ~0  (ripgrep on disk — no cloud LLM)
  Saved:             ~6,011  (= baseline − spent)

Note: MCP in Agent chat still uses Cursor tokens for rules + your message +
agent reply. Only the executor row above is local / cheap.